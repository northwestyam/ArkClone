#include "arkclone.h"
#include <SDL/SDL.h>
#include "graphics.h"
#include "paddle.h"

namespace {
	const int kFPS = 60;
}

Arkclone::Arkclone() {
	SDL_Init(SDL_INIT_EVERYTHING);
	SDL_ShowCursor(SDL_DISABLE);
	eventLoop();
}

Arkclone::~Arkclone() {
	SDL_Quit();
}

void Arkclone::eventLoop() {
	Graphics graphics;
	SDL_Event event;
	Paddle paddle("graphics/player.bmp",0,0,102,22);

	bool running = true;
	int last_update_time = SDL_GetTicks();
	while (running) {
		const int start_time = SDL_GetTicks();
		while(SDL_PollEvent(&event)) {
			switch (event.type) {
				case SDL_KEYDOWN:
					break;
				case SDL_KEYUP:
					break;
				default:
			}
		}
	}

}

void Arkclone::update(int elapsed_time_ms) {

}

void Arkclone::draw(Graphics& graphics) {

}
